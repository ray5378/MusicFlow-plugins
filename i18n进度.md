# i18n 国际化进度

> MusicFlow-plugins（内置/外置歌单插件仓库）的 i18n 需求与进度。机制由主仓库提供（默认中文，向后兼容）。

## 整体任务需求

- 主仓库已建立 **插件侧 i18n 字典**：`PluginManifest.i18n`（`{ zh?, en? }`，默认中文、**zh 可省略只须补 en**），覆盖 `name / description / platformLabels / groups / configSchema` 的 `label / help / options`（`docs/PLUGIN_DEV.md` §3.4）。
- 本仓库任务：为各插件 `configSchema` 字段（`label / help / options`）与平台标签等补 **English** 文案，使英文界面下插件配置不再显示中文默认值。
- 与宿主注入开关（`schedules` / `batchParallel`）类似，i18n 无需改动插件执行逻辑，仅扩展 manifest 声明。

## 当前进度：已完成（全部 7 个插件已补 `i18n.en`）

- **已完成**：为以下 7 个插件 manifest 补 `i18n.en`（`name` / `description` / `groups` / `fields` 的 `label / help / options` + go-music-dl 的 `platformLabels`）：
  - `qq-chart`、`kugou-chart`、`netease-chart`、`go-music-dl`、`listenbrainz`、`lastfm`、`playlist-cleaner`。
- 约定：**默认文案保持中文**（权威副本不变），`i18n.en` 仅新增英文；zh/en 键**完全一致**，未覆盖关键字自动回退中文默认文案；已通过 `JSON.parse` 校验全部 7 份 `plugin.json` 为合法 JSON，scripts/check.mjs 全量通过。
- **已完成**：全部 7 个插件补 i18n.en.documentation（详情弹窗「处理逻辑」Markdown 英文版；playlist-cleaner 原无 documentation 故跳过）与 batchParallel 开关文案；playlist-cleaner 另补宿主注入的 scheduleEnabled / runOnBoot 文案（其余 6 个已在 configSchema 声明并含 i18n）。

- **已完成（运行时生效）**：把上述 i18n.en（及缺失的 documentation）同步进 7 个插件的 **index.js manifest**——沙箱以 index.js 的 __mfPlugin.manifest 为权威（MusicFlow backend/src/plugins/sandbox.ts），只写 plugin.json 英文界面读不到。scripts/check.mjs 全量通过。

- dist 说明：`dist/*.tar.gz` 为 `scripts/pack.sh` 的构建产物（gitignore、不入库），语义化发布时由 `pack.sh` 重新打包即可带上 `i18n.en`，无需手改 dist。

## 关联仓库

- 机制说明见 `MusicFlow/PLUGIN_DEV.md` §3.4 与 `MusicFlow/i18n进度.md`。